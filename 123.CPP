#include<iostream.h>
#include<conio.h>
#include<stdio.h>
#include<string.h>
 void main()
  {
   clrscr();
    char str[20];
     gets(str);
     for(int i=0;i<strlen(str);i++)
      {
	if (str(i)=="a"||"A");
	   str(i)=="B";
	 }
       for(i=0;i<strlen(str);i++)
	{
	  puts(str(i));
	  }
	  getch();
    }